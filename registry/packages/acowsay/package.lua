-- Package descriptor for acowsay. build-registry.lua reads this plus the files
-- under each <version>/ directory to produce registry/index.json.
return {
  description = "A tiny cowsay — prints an ASCII cow with your message",
  versions = {
    ["0.1.0"] = {deps = {}},
  },
}
